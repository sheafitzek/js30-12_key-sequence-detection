const feature = (function() { // "feature" is a placeholder for an element or section of html
	config: {
		container: document.querySelector(`.targetClassOrId`); // "targetClassOrId" is the class (or id) of the "feature" we are working on
	},

	init(config) {
		
	},

	publish() {
		
	},

	subscribe() {
		
	},

	return {
		init: init,
	};

})();

window.onload = app.init;
